apiVersion: v1
kind: Service
metadata:
  name: {{ include "devdocify.fullname" . }}
  labels:
    {{- include "devdocify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devdocify.selectorLabels" . | nindent 4 }}
